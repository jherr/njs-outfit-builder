/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "accomplished-narwhal-57.convex.cloud",
      "adamant-dog-760.convex.cloud",
    ],
  },
};

module.exports = nextConfig;
